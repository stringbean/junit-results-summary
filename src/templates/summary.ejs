<html>
<head>
    <title>JUnit Summary Report</title>

    <style>
        body {
            font-family: -apple-system,BlinkMacSystemFont,"Segoe UI",Helvetica,Arial,sans-serif,"Apple Color Emoji","Segoe UI Emoji"
        }

        .summary-field {
            font-weight: bold;
        }

        table {
            width: 100%;
        }

        tr.total {
            font-weight: bold;
        }

        td.count,
        td.duration,
        td.status {
            width: 4em;
            text-align: right;
        }

        td.duration {
            width: 6em;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        table.projectReport.tests .suite {
            font-family: monospace;
        }

        .status .pass {
            color: #008b00;
        }

        .status .fail {
            color: #8b0000;
        }

        @media (prefers-color-scheme: dark) {
            body {
                background: #0D1117;
                color: #c9d1d9;
            }

            tr:nth-child(even) {
                background-color: #242424;
            }

            a {
                color: #58a6ff;
            }
        }
    </style>
</head>
</html>